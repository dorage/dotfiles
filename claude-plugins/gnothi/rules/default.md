# gnothi: 모델 공통 (전용 룰이 없을 때)

이 세션의 모델에 맞는 전용 룰 파일이 없거나, 모델을 판별하지 못했다.

- 위의 `current model` 이 `unknown` 이면 모델을 아직 모른다. 자신의 능력을 전제로 한 판단(대규모 리팩토링 착수, 서브에이전트 대량 위임)은 마스터에게 모델을 확인한 뒤에 한다.
- 되돌리기 어려운 작업(삭제, 마이그레이션, 외부 서비스 호출)은 모델과 무관하게 착수 전에 마스터 확인을 받는다.
